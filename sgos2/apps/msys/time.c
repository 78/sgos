#include <sgos.h>
#include <stdio.h>
#include <api.h>
#include "debug.h"
#include <time.h>

clock_t times(struct tms *buf)
{
	return -1;
}

int gettimeofday ( struct timeval * tv , struct timezone * tz )
{
	return -1;
}
